const { gsap } = require('gsap');
const { Observer } = require('gsap/Observer');

// Let's check how Observer is defined or test in browser via CDP
console.log('Observer available:', typeof Observer);
